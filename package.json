{
  "name": "morning-meeting-report",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite --host 127.0.0.1 --port 3000",
    "build": "vite build",
    "build:daily-report": "BUILD_TARGET=daily-report GITHUB_PAGES=true PAGES_REPO=daily-report-tool vite build && node -e \"const fs=require('fs');fs.copyFileSync('dist/daily-report.html','dist/index.html')\"",
    "preview": "vite preview --host 127.0.0.1 --port 3000",
    "generate": "node scripts/generate-site-data.mjs"
  },
  "dependencies": {
    "@supabase/supabase-js": "^2.106.2",
    "exceljs": "^4.4.0"
  },
  "devDependencies": {
    "vite": "^6.3.5"
  }
}
